{
  "name": "reticle-archive-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker that serves .rtla archives from R2 over HTTP Range, cached and CORS-locked to the Pages origin.",
  "scripts": {
    "test": "node --test",
    "dev": "wrangler dev",
    "check": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "4.82.2"
  }
}
