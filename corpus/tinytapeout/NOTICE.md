# Tiny Tapeout import-hardening corpus

This directory proves the hardened GDSII importer never panics or hangs and always returns a clean document or a clean error. It is regenerated by `cargo run -p reticle-io --example gen_tinytapeout_corpus --features corpus-tools`.

## Files

| file | provenance |
|------|------------|
| `malformed_empty.gds` | synthesized: zero-length file (Err: unsupported model operation: malformed or unreadable input) |
| `malformed_not_gds.gds` | synthesized: non-GDS bytes (no HEADER record) (Err: unsupported model operation: malformed or unreadable input) |
| `malformed_truncated.gds` | synthesized: valid HEADER then an abrupt cut mid-stream (Err: unsupported model operation: malformed or unreadable input) |
| `malformed_bad_record_len.gds` | synthesized: a record header claiming length 3 (below the 4-byte minimum) (Err: unsupported model operation: malformed or unreadable input) |
| `malformed_absurd_xy_count.gds` | synthesized: an XY record header claiming far more bytes than follow (Err: unsupported model operation: malformed or unreadable input) |
| `malformed_cyclic_sref.gds` | synthesized: two structs that reference each other (A->B->A) (Ok: 2 cells, 0 warnings) |
| `malformed_dangling_sref.gds` | synthesized: an SREF to a struct name that is never defined (Ok: 1 cells, 0 warnings) |
| `degenerate_boundary.gds` | synthesized: a good boundary plus a degenerate 2-vertex boundary that imports with a warning, not an error (Ok: 1 cells, 1 warnings) |
| `real_tinytapeout_min.gds` | minimized from Tiny Tapeout 03 design 'adder': 3 real SkyWater leaf cells (sky130_fd_sc_hd__and2_1, sky130_fd_sc_hd__buf_1, sky130_fd_sc_hd__conb_1) under a small synthesized top; re-exported with dates normalized for reproducibility. Apache-2.0. (Ok: 4 cells, 0 warnings) |

## Real-sample provenance

The `real_*` sample is derived from a real, published Tiny Tapeout 03 submitted design fetched by `scripts/fetch-tinytapeout-gds.ps1` from `https://github.com/TinyTapeout/tinytapeout-03` (Apache-2.0). Only a few real SkyWater standard-cell definitions are kept, under a small synthesized top cell, and re-exported so the committed file stays tiny; the cell names, layer/datatype pairs, coordinates, and record mix are the real design's. The full multi-megabyte tiles are intentionally not committed.

The `malformed_*` samples are fully synthesized in-repo (no third-party content) to exercise distinct parser hazards.
