# Tauri-generated JSON schemas for capabilities/tauri.conf.json IDE
# autocompletion, regenerated by `tauri-build`'s ACL step on every build
# (matches the upstream `create-tauri-app` template's default ignore for this
# exact path). The root `**/target` rule does not cover this: `tauri-build`
# writes it into the crate directory, not into the target/OUT_DIR.
/gen/schemas/
